/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add other Next.js configurations here if needed
  // For example, to enable output standalone for Docker deployment:
  output: 'standalone',
};

module.exports = nextConfig;
