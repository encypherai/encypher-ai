# Railway deployment configuration

# Web server (FastAPI application via Gunicorn for proper process management)
web: gunicorn app.main:app -w ${WEB_CONCURRENCY:-4} -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --timeout 120 --graceful-timeout 30 --keep-alive 5

# Background worker (certificate monitoring - to be implemented)
# worker: python scripts/certificate_monitor.py
