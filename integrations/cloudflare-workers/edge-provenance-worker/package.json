{
  "name": "encypher-edge-provenance-worker",
  "version": "1.0.0",
  "description": "Encypher Edge Provenance Worker - embed copy-paste-survivable content provenance markers at the CDN edge",
  "type": "module",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js"
  },
  "keywords": ["cloudflare-workers", "c2pa", "provenance", "content-authentication"],
  "license": "UNLICENSED",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
