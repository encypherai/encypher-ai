version: "3.9"

# Override for UX/UI testing with Puppeteer
# Uses port 8888 to avoid conflict with main dev stack (Traefik on 8080)
# Skips enterprise-api/enterprise-db since we use the running dev stack on port 9000

services:
  wp-db:
    image: mysql:8.0
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wordpress
      MYSQL_PASSWORD: wordpress
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - wordpress-test-db-data:/var/lib/mysql

  wordpress:
    image: wordpress:6.5-php8.2
    depends_on:
      wp-db:
        condition: service_healthy
    ports:
      - "8888:80"
    environment:
      WORDPRESS_DB_HOST: wp-db:3306
      WORDPRESS_DB_USER: wordpress
      WORDPRESS_DB_PASSWORD: wordpress
      WORDPRESS_DB_NAME: wordpress
      WORDPRESS_DEBUG: "1"
      WORDPRESS_CONFIG_EXTRA: |
        define( 'WP_HOME', 'http://localhost:8888' );
        define( 'WP_SITEURL', 'http://localhost:8888' );
    volumes:
      - wordpress-test-data:/var/www/html
      - ./plugin/encypher-provenance:/var/www/html/wp-content/plugins/encypher-provenance
    extra_hosts:
      - "host.docker.internal:host-gateway"

  wp-cli:
    image: wordpress:cli
    depends_on:
      wordpress:
        condition: service_started
    entrypoint: ["wp", "--allow-root"]
    working_dir: /var/www/html
    environment:
      WORDPRESS_DB_HOST: wp-db:3306
      WORDPRESS_DB_USER: wordpress
      WORDPRESS_DB_PASSWORD: wordpress
      WORDPRESS_DB_NAME: wordpress
    volumes:
      - wordpress-test-data:/var/www/html
      - ./plugin/encypher-provenance:/var/www/html/wp-content/plugins/encypher-provenance

volumes:
  wordpress-test-db-data:
  wordpress-test-data:
