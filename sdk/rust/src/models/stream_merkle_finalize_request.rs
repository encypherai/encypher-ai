/*
 * Encypher Enterprise API
 *
 * C2PA-compliant content signing and verification infrastructure for publishers, legal/finance firms, AI labs, and enterprises
 *
 * The version of the OpenAPI document: 1.0.1
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// StreamMerkleFinalizeRequest : Request to finalize a streaming Merkle session and compute the final root.  This completes the tree construction, computes the final root hash, and optionally embeds a C2PA manifest into the full document.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct StreamMerkleFinalizeRequest {
    /// Session ID to finalize
    #[serde(rename = "session_id")]
    pub session_id: String,
    /// Whether to embed C2PA manifest into the final document
    #[serde(rename = "embed_manifest", skip_serializing_if = "Option::is_none")]
    pub embed_manifest: Option<bool>,
    /// Manifest mode: full, lightweight_uuid, hybrid
    #[serde(rename = "manifest_mode", skip_serializing_if = "Option::is_none")]
    pub manifest_mode: Option<String>,
    /// C2PA action type: c2pa.created or c2pa.edited
    #[serde(rename = "action", skip_serializing_if = "Option::is_none")]
    pub action: Option<String>,
}

impl StreamMerkleFinalizeRequest {
    /// Request to finalize a streaming Merkle session and compute the final root.  This completes the tree construction, computes the final root hash, and optionally embeds a C2PA manifest into the full document.
    pub fn new(session_id: String) -> StreamMerkleFinalizeRequest {
        StreamMerkleFinalizeRequest {
            session_id,
            embed_manifest: None,
            manifest_mode: None,
            action: None,
        }
    }
}

