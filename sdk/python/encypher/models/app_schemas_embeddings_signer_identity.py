# coding: utf-8

"""
    Encypher Enterprise API

    C2PA-compliant content signing and verification infrastructure for publishers, legal/finance firms, AI labs, and enterprises

    The version of the OpenAPI document: 1.0.2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class AppSchemasEmbeddingsSignerIdentity(BaseModel):
    """
    Signer identity and trust chain information.  TEAM_165: When an org starts with a self-signed key managed by Encypher, micro markers are already cryptographically bound to that key. If the org later obtains a CA-signed certificate (via /byok/certificates), the trust chain upgrades automatically — no re-signing needed.
    """ # noqa: E501
    organization_id: StrictStr = Field(description="Organization identifier")
    organization_name: Optional[StrictStr] = None
    certificate_status: StrictStr = Field(description="Certificate lifecycle: none, pending, active, expired, revoked")
    ca_backed: Optional[StrictBool] = Field(default=False, description="True if the org certificate chains to a trusted CA (not self-signed)")
    issuer: Optional[StrictStr] = None
    certificate_expiry: Optional[datetime] = None
    trust_level: Optional[StrictStr] = Field(default='self_signed', description="Trust level: 'ca_verified' (chains to C2PA-trusted CA), 'self_signed' (Encypher-managed key), 'none' (no certificate)")
    __properties: ClassVar[List[str]] = ["organization_id", "organization_name", "certificate_status", "ca_backed", "issuer", "certificate_expiry", "trust_level"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AppSchemasEmbeddingsSignerIdentity from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if organization_name (nullable) is None
        # and model_fields_set contains the field
        if self.organization_name is None and "organization_name" in self.model_fields_set:
            _dict['organization_name'] = None

        # set to None if issuer (nullable) is None
        # and model_fields_set contains the field
        if self.issuer is None and "issuer" in self.model_fields_set:
            _dict['issuer'] = None

        # set to None if certificate_expiry (nullable) is None
        # and model_fields_set contains the field
        if self.certificate_expiry is None and "certificate_expiry" in self.model_fields_set:
            _dict['certificate_expiry'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AppSchemasEmbeddingsSignerIdentity from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "organization_id": obj.get("organization_id"),
            "organization_name": obj.get("organization_name"),
            "certificate_status": obj.get("certificate_status"),
            "ca_backed": obj.get("ca_backed") if obj.get("ca_backed") is not None else False,
            "issuer": obj.get("issuer"),
            "certificate_expiry": obj.get("certificate_expiry"),
            "trust_level": obj.get("trust_level") if obj.get("trust_level") is not None else 'self_signed'
        })
        return _obj
