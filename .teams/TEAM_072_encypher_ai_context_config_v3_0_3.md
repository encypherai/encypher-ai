# TEAM_072 — encypher-ai context configuration v3.0.3

## Summary
Ship `encypher-ai==3.0.3` with production verification fixes and configurable C2PA `@context` handling via environment variables so microservices can control emitted and accepted schema contexts.

## Work Log
- Created PRD for v3.0.3 context configurability and verification fixes.

## Notes
- Goal: production microservices rely on published packages (no monorepo path overrides in production).
