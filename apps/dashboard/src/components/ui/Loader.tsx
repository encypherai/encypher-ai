/**
 * Re-export EncypherLoader from @encypher/icons as Loader for backwards compatibility.
 * Uses the corrected 24x24 viewBox with centered rotation axis.
 */
export { EncypherLoader as Loader } from '@encypher/icons';
